diff -ur src.old/sys/sys/tty.h src/sys/sys/tty.h
--- src.old/sys/sys/tty.h	2004-08-03 08:37:35.000000000 +0200
+++ src/sys/sys/tty.h	2004-08-03 08:53:33.000000000 +0200
@@ -51,8 +51,15 @@
 #define	_SYS_TTY_H_
 
 #include <sys/termios.h>
-#include <sys/queue.h>
-#include <sys/selinfo.h>
+#include <sys/queue.h> /* SLIST_ENTRY */
+#ifndef SLIST_ENTRY
+#define	SLIST_ENTRY(type)						\
+struct {								\
+	struct type *sle_next;	/* next element */			\
+}
+#endif
+#include <sys/types.h>
+#include <sys/ioctl.h> /* winsize struct */
 
 /*
  * Clists are character lists, which is a variable length linked list
@@ -93,8 +100,6 @@
 	struct	pgrp *t_pgrp;		/* Foreground process group. */
 	struct	session *t_session;	/* Enclosing session. */
 	struct  sigio *t_sigio;		/* Information for async I/O. */
-	struct	selinfo t_rsel;		/* Tty read/oob select. */
-	struct	selinfo t_wsel;		/* Tty write select. */
 	struct	termios t_termios;	/* Termios state. */
 	struct	winsize t_winsize;	/* Window size. */
 					/* Start output. */
