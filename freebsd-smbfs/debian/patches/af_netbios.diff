--- a/contrib/smbfs/lib/smb/nb_name.c
+++ b/contrib/smbfs/lib/smb/nb_name.c
@@ -61,7 +61,7 @@
 	if (snb == NULL)
 		return ENOMEM;
 	bzero(snb, slen);
-	snb->snb_family = AF_NETBIOS;
+	snb->snb_family = 6;
 	snb->snb_len = slen;
 	*dst = snb;
 	return 0;
