- find out why CD access is so slow (hw.ata.atapi_dma=1 in loader.conf)
- nuke the nonsense question about X server resolutions
- use cowloop
  - port cloop-utils
  - find out how the geom_uzip kernel module works
  - prepare a minimal initrd (initrd-tools)
  - add packages that didn't fit before (full kde, gimp, koffice..)
