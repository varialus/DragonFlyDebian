- use cowloop
  - port cloop-utils
  - find out how the geom_uzip kernel module works
  - prepare a minimal initrd (initrd-tools)
- try if removing /usr/bin/kpersonalizer works to get rid of the idem problem
  (instead of using ging_home.tar.gz)
