--- a/sbin/mount_autofs/mount_autofs.c
+++ b/sbin/mount_autofs/mount_autofs.c
@@ -69,8 +69,8 @@
 mymount(
 	const char *type,
 	const char *dir,
-	int flags __unused,
-	void *data __unused
+	int flags,
+	void *data
 )
 {
 	struct iovec iov[4], *iovp;
