kfreebsd-common (0.2.4) unreleased; urgency=low

  * Fix /dev/cdrom symlink handling.

 -- Robert Millan <rmh@aybabtu.com>  Fri, 28 Apr 2006 18:54:49 +0200

kfreebsd-common (0.2.3) unreleased; urgency=low

  * kern.module_path component separator is ';', not ':'.

 -- Robert Millan <rmh@aybabtu.com>  Fri, 28 Apr 2006 15:37:17 +0200

kfreebsd-common (0.2.2) unreleased; urgency=low

  * Set /dev/cdrom symlink.
  * chgrp disk /dev/ata.
  * Set kern.module_path=/lib/modules/6.0-1-686:/boot/kernel.
  * control: Depend on module-init-tools|kldutils.

 -- Robert Millan <rmh@aybabtu.com>  Thu, 27 Apr 2006 16:48:23 +0200

kfreebsd-common (0.2.1) unreleased; urgency=low

  * templates: s/dpkg-reconfigure kfreebsd-config/dpkg-reconfigure kfreebsd-common/g.
  * Fix /etc/mtab symlink in both init.d and postinst.

 -- Robert Millan <rmh@aybabtu.com>  Tue,  4 Apr 2006 14:49:55 +0200

kfreebsd-common (0.2.0) unreleased; urgency=low

  * Overhaul module name lists to make them dynamic.  A module_descriptions
    list is maintained with known descriptions (when there's no match, no
    description is provided).
  * Use "high" priority for network drivers.
  * Fix a pair of broken regexps.
  * Remove /etc/init.d/networking hack.  It brought more problems than
    it actualy solved.
  * Exclude acpi|linux|linprocfs modules from automatic list.
  * copyright: Remove old FSF address.

 -- Robert Millan <rmh@aybabtu.com>  Wed, 15 Mar 2006 11:59:36 +0100

kfreebsd-common (0.1.1) unreleased; urgency=low

  * init.d: Fix /dev/da[0-9]* too.

 -- Robert Millan <rmh@aybabtu.com>  Thu,  2 Mar 2006 11:42:58 +0100

kfreebsd-common (0.1.0) unreleased; urgency=low

  * Rename to kfreebsd-common.
  * init.d: New. Fix /dev permissions on startup.

  * templates:  Improve/update the descriptions.
  * control (Depends):  Add kfreebsd-image (needed for kldload).
  * postinst:  In get_modules(), return safely if module list does not exist.

 -- Robert Millan <rmh@aybabtu.com>  Fri, 17 Feb 2006 18:45:55 +0100

kfreebsd-config (0.0.4) unreleased; urgency=low

  * Remove nfsclient (doesn't autoload as module) and ums (will stay in kernel
    image) from "other" list.
  * config:  Lower priority of network template (to "medium").
  * Don't mess with network interfaces when they aren't being used.
    - control (Depends): Nuke netbase.
    - postinst: Only invoke /etc/init.d/networking when it exists.
  * Rise Priority to "required".
  * templates:  Remove obsolete drivers snd_sb8 and snd_sb16.  Improve
    description of snd_sbc which superceeds them.
  * Add snd_probe command to detect soundcard, and refer to it in sound
    template.
    - snd_probe
    - dirs
    - install
    - templates
  * postinst:  Fix flaw in the routine that determines wether to unload modules.
    Now modules are unloaded only when required by the logic described in the
    comments.  As for "acpi", explicitly avoid to mess with this module, since
    it can only be handled by the kernel loader.

 -- Robert Millan <rmh@aybabtu.com>  Tue,  2 Aug 2005 13:48:10 +0200

kfreebsd-config (0.0.3) unreleased; urgency=low

  * postinst: Actualy, unload modules when they have been removed, not when they
    have *not* been.
  * Invoke /etc/init.d/networking to make sure that messing with network modules
    doesn't kill the interface.
    - postinst
    - control: Depend on netbase.
    - templates (network): Add warning about broken /etc/init.d/networking
    files (dhclient).

 -- Robert Millan <rmh@aybabtu.com>  Mon,  1 Aug 2005 15:48:26 +0200

kfreebsd-config (0.0.2) unreleased; urgency=low

  * control: Fill in Description field (I'm so lame).
  * templates: Clarify in that snd_ich may also apply to integrated SiS cards.
  * postinst: Only unload modules that are not listed to be loaded later!

 -- Robert Millan <rmh@aybabtu.com>  Mon,  1 Aug 2005 15:03:25 +0200

kfreebsd-config (0.0.1) unreleased; urgency=low

  * Initial release.

 -- Robert Millan <rmh@aybabtu.com>  Mon,  1 Aug 2005 07:03:29 +0200

